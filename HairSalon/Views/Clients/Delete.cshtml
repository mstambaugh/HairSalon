@{
  Layout = "_Layout";
}

@model HairSalon.Models.Client

<h2>Are you sure you want to delete this client? Maybe offer them a new stylist?</h2>

@Html.DisplayNameFor(model => model.ClientName): @Html.DisplayFor(model => model.ClientName)
@using (Html.BeginForm())
{
    <input type="submit" value="Delete" class="btn btn-default"/>
}
@Html.ActionLink("Back to client list", "Index")